# 第九章：配置系统详解

## 9.1 配置系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      Config (单例)                               │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  _config: AppConfig                                       │  │
│  │  - llm: Dict[str, LLMSettings]                           │  │
│  │  - browser_config: BrowserSettings                        │  │
│  │  - search_config: SearchSettings                          │  │
│  │  - mcp_config: MCPSettings                                │  │
│  │  - sandbox: SandboxSettings                               │  │
│  │  - run_flow_config: RunflowSettings                       │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ config.toml     │  │ mcp.json        │  │ 环境变量        │
│ (主配置)        │  │ (MCP服务器配置)  │  │ (API密钥等)     │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

## 9.2 配置类定义

**文件位置**：`app/config.py`

### 9.2.1 LLM 配置

```python
class LLMSettings(BaseModel):
    """大语言模型配置"""

    model: str                              # 模型名称
    base_url: str                           # API 基础 URL
    api_key: str                            # API 密钥
    max_tokens: int = 4096                  # 最大 token 数
    max_input_tokens: Optional[int] = None  # 最大输入 token
    temperature: float = 1.0                 # 采样温度
    api_type: str                            # API 类型: azure/openai/ollama
    api_version: str                         # Azure API 版本
```

### 9.2.2 浏览器配置

```python
class BrowserSettings(BaseModel):
    """浏览器配置"""

    headless: bool = False                  # 无头模式
    disable_security: bool = True           # 禁用安全限制
    extra_chromium_args: List[str] = []     # 额外 Chromium 参数
    chrome_instance_path: Optional[str] = None   # Chrome 实例路径
    wss_url: Optional[str] = None           # WebSocket URL
    cdp_url: Optional[str] = None           # CDP URL
    proxy: Optional[ProxySettings] = None   # 代理设置
    max_content_length: int = 2000          # 最大内容长度
```

### 9.2.3 搜索配置

```python
class SearchSettings(BaseModel):
    """搜索引擎配置"""

    engine: str = "Google"                   # 主搜索引擎
    fallback_engines: List[str] = [         # 备用引擎
        "DuckDuckGo", "Baidu", "Bing"
    ]
    retry_delay: int = 60                   # 重试延迟(秒)
    max_retries: int = 3                   # 最大重试次数
    lang: str = "en"                        # 搜索语言
    country: str = "us"                     # 搜索国家
```

### 9.2.4 MCP 配置

```python
class MCPServerConfig(BaseModel):
    """单个 MCP 服务器配置"""

    type: str                                # 连接类型: sse/stdio
    url: Optional[str] = None               # SSE 连接 URL
    command: Optional[str] = None            # stdio 命令
    args: List[str] = []                    # stdio 参数

class MCPSettings(BaseModel):
    """MCP 全局配置"""

    server_reference: str = "app.mcp.server"  # 服务器模块引用
    servers: Dict[str, MCPServerConfig] = {} # 服务器配置映射
```

### 9.2.5 沙箱配置

```python
class SandboxSettings(BaseModel):
    """执行沙箱配置"""

    use_sandbox: bool = False               # 是否启用沙箱
    image: str = "python:3.12-slim"        # Docker 镜像
    work_dir: str = "/workspace"            # 工作目录
    memory_limit: str = "512m"              # 内存限制
    cpu_limit: float = 1.0                   # CPU 限制
    timeout: int = 300                      # 命令超时
    network_enabled: bool = False           # 网络访问
```

## 9.3 Config 单例实现

```python
class Config:
    """全局配置单例"""

    _instance = None
    _lock = threading.Lock()
    _initialized = False

    def __new__(cls):
        if cls._instance is None:
            with cls._lock:
                if cls._instance is None:
                    cls._instance = super().__new__(cls)
        return cls._instance

    def __init__(self):
        if not self._initialized:
            with self._lock:
                if not self._initialized:
                    self._config = None
                    self._load_initial_config()
                    self._initialized = True
```

### 9.3.1 配置加载

```python
@staticmethod
def _get_config_path() -> Path:
    """获取配置文件路径"""
    root = PROJECT_ROOT
    config_path = root / "config" / "config.toml"
    if config_path.exists():
        return config_path
    example_path = root / "config" / "config.example.toml"
    if example_path.exists():
        return example_path
    raise FileNotFoundError("No configuration file found")

def _load_config(self) -> dict:
    """加载 TOML 配置"""
    config_path = self._get_config_path()
    with config_path.open("rb") as f:
        return tomllib.load(f)

def _load_initial_config(self):
    """解析并初始化配置"""
    raw_config = self._load_config()

    # 处理 LLM 配置
    base_llm = raw_config.get("llm", {})
    default_settings = {
        "model": base_llm.get("model"),
        "base_url": base_llm.get("base_url"),
        "api_key": base_llm.get("api_key"),
        "max_tokens": base_llm.get("max_tokens", 4096),
        "max_input_tokens": base_llm.get("max_input_tokens"),
        "temperature": base_llm.get("temperature", 1.0),
        "api_type": base_llm.get("api_type", ""),
        "api_version": base_llm.get("api_version", ""),
    }

    # 处理浏览器配置
    browser_config = raw_config.get("browser", {})
    if browser_config:
        browser_settings = BrowserSettings(**browser_config)
    else:
        browser_settings = None

    # 处理搜索配置
    search_config = raw_config.get("search", {})
    if search_config:
        search_settings = SearchSettings(**search_config)
    else:
        search_settings = None

    # 处理沙箱配置
    sandbox_config = raw_config.get("sandbox", {})
    if sandbox_config:
        sandbox_settings = SandboxSettings(**sandbox_config)
    else:
        sandbox_settings = SandboxSettings()

    # 处理 MCP 配置
    mcp_config = raw_config.get("mcp", {})
    if mcp_config:
        mcp_config["servers"] = MCPSettings.load_server_config()
        mcp_settings = MCPSettings(**mcp_config)
    else:
        mcp_settings = MCPSettings(servers=MCPSettings.load_server_config())

    # 构建完整配置
    self._config = AppConfig(
        llm={
            "default": default_settings,
            **{
                name: {**default_settings, **override}
                for name, override in base_llm.items()
                if isinstance(override, dict)
            }
        },
        sandbox=sandbox_settings,
        browser_config=browser_settings,
        search_config=search_settings,
        mcp_config=mcp_settings,
        run_flow_config=RunflowSettings(**raw_config.get("runflow", {})),
    )
```

## 9.4 MCP 服务器配置加载

```python
class MCPSettings(BaseModel):
    servers: Dict[str, MCPServerConfig] = {}

    @classmethod
    def load_server_config(cls) -> Dict[str, MCPServerConfig]:
        """从 JSON 文件加载 MCP 服务器配置"""
        config_path = PROJECT_ROOT / "config" / "mcp.json"

        try:
            if not config_path.exists():
                return {}

            with config_path.open() as f:
                data = json.load(f)
                servers = {}

                for server_id, server_config in data.get("mcpServers", {}).items():
                    servers[server_id] = MCPServerConfig(
                        type=server_config["type"],
                        url=server_config.get("url"),
                        command=server_config.get("command"),
                        args=server_config.get("args", []),
                    )
                return servers
        except Exception as e:
            raise ValueError(f"Failed to load MCP server config: {e}")
```

## 9.5 配置文件示例

### config.toml

```toml
[llm]
model = "claude-3-7-sonnet"
base_url = "https://api.anthropic.com/v1/"
api_key = "YOUR_API_KEY"
max_tokens = 4096
max_input_tokens = 100000
temperature = 1.0
api_type = "openai"  # openai, azure, aws
api_version = ""

[browser]
headless = false
disable_security = true
max_content_length = 2000

[search]
engine = "Google"
lang = "en"
country = "us"

[mcp]
server_reference = "app.mcp.server"

[runflow]
use_data_analysis_agent = false
```

### mcp.json

```json
{
  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/path/to/directory"
      ]
    },
    "github": {
      "type": "sse",
      "url": "http://localhost:3000/sse"
    }
  }
}
```

## 9.6 配置访问方式

```python
from app.config import config

# LLM 配置
llm_settings = config.llm["default"]
model = llm_settings.model

# 浏览器配置
browser_config = config.browser_config
headless = browser_config.headless if browser_config else False

# 搜索配置
search_config = config.search_config
engine = search_config.engine if search_config else "Google"

# MCP 配置
mcp_config = config.mcp_config
servers = mcp_config.servers

# 沙箱配置
sandbox = config.sandbox
use_sandbox = sandbox.use_sandbox

# 工作空间路径
workspace = config.workspace_root
```

## 9.7 环境变量支持

```python
import os

# 从环境变量读取 API Key
api_key = os.getenv("OPENAI_API_KEY") or config.llm["default"].api_key

# 或使用 pydantic-settings
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    openai_api_key: str
    anthropic_api_key: str
    # ...

    class Config:
        env_file = ".env"
```
