# 第十章：数据模型详解

## 10.1 数据模型总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        Schema 模块                               │
│  app/schema.py                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │    Role    │  │ToolChoice   │  │AgentState   │              │
│  │  (消息角色) │  │(工具选择)   │  │(Agent状态)  │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                      Message                            │    │
│  │  - role: ROLE_TYPE                                     │    │
│  │  - content: Optional[str]                              │    │
│  │  - tool_calls: Optional[List[ToolCall]]                │    │
│  │  - base64_image: Optional[str]                         │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                       Memory                            │    │
│  │  - messages: List[Message]                            │    │
│  │  - max_messages: int                                  │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │  Function   │  │  ToolCall   │  │  ToolResult │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 10.2 枚举类型

**文件位置**：`app/schema.py`

### 10.2.1 Role 角色枚举

```python
class Role(str, Enum):
    """消息角色选项"""
    SYSTEM = "system"      # 系统消息
    USER = "user"         # 用户消息
    ASSISTANT = "assistant"  # 助手消息
    TOOL = "tool"         # 工具响应消息

ROLE_VALUES = tuple(role.value for role in Role)
ROLE_TYPE = Literal[ROLE_VALUES]
```

### 10.2.2 ToolChoice 工具选择枚举

```python
class ToolChoice(str, Enum):
    """工具调用选择策略"""
    NONE = "none"         # 不使用工具
    AUTO = "auto"         # 自动选择
    REQUIRED = "required" # 必须使用工具

TOOL_CHOICE_VALUES = tuple(choice.value for choice in ToolChoice)
TOOL_CHOICE_TYPE = Literal[TOOL_CHOICE_VALUES]
```

### 10.2.3 AgentState Agent 状态枚举

```python
class AgentState(str, Enum):
    """Agent 执行状态"""
    IDLE = "IDLE"         # 空闲状态
    RUNNING = "RUNNING"   # 运行中
    FINISHED = "FINISHED" # 已完成
    ERROR = "ERROR"       # 错误状态
```

## 10.3 Function 工具函数模型

```python
class Function(BaseModel):
    """工具函数定义"""
    name: str       # 函数名称
    arguments: str # JSON 格式的参数
```

## 10.4 ToolCall 工具调用模型

```python
class ToolCall(BaseModel):
    """LLM 生成的工具调用"""
    id: str = "function"  # 调用 ID
    type: str = "function"
    function: Function     # 函数信息

# 示例
{
    "id": "call_abc123",
    "type": "function",
    "function": {
        "name": "web_search",
        "arguments": '{"query": "Python async"}'
    }
}
```

## 10.5 Message 消息模型

```python
class Message(BaseModel):
    """对话消息"""

    role: ROLE_TYPE                              # 消息角色
    content: Optional[str] = None               # 消息内容
    tool_calls: Optional[List[ToolCall]] = None  # 工具调用列表
    name: Optional[str] = None                 # 工具名称(用于 tool 角色)
    tool_call_id: Optional[str] = None         # 工具调用 ID
    base64_image: Optional[str] = None          # Base64 编码的图像
```

### 10.5.1 工厂方法

```python
@classmethod
def user_message(
    cls, content: str, base64_image: Optional[str] = None
) -> "Message":
    """创建用户消息"""
    return cls(role=Role.USER, content=content, base64_image=base64_image)

@classmethod
def system_message(cls, content: str) -> "Message":
    """创建系统消息"""
    return cls(role=Role.SYSTEM, content=content)

@classmethod
def assistant_message(
    cls, content: Optional[str] = None, base64_image: Optional[str] = None
) -> "Message":
    """创建助手消息"""
    return cls(role=Role.ASSISTANT, content=content, base64_image=base64_image)

@classmethod
def tool_message(
    cls, content: str, name, tool_call_id: str, base64_image: Optional[str] = None
) -> "Message":
    """创建工具响应消息"""
    return cls(
        role=Role.TOOL,
        content=content,
        name=name,
        tool_call_id=tool_call_id,
        base64_image=base64_image,
    )
```

### 10.5.2 从工具调用创建

```python
@classmethod
def from_tool_calls(
    cls,
    tool_calls: List[Any],
    content: Union[str, List[str]] = "",
    base64_image: Optional[str] = None,
    **kwargs,
) -> "Message":
    """从原始工具调用创建消息"""
    formatted_calls = [
        {
            "id": call.id,
            "function": call.function.model_dump(),
            "type": "function"
        }
        for call in tool_calls
    ]
    return cls(
        role=Role.ASSISTANT,
        content=content,
        tool_calls=formatted_calls,
        base64_image=base64_image,
        **kwargs,
    )
```

### 10.5.3 转换为字典

```python
def to_dict(self) -> dict:
    """转换为字典格式"""
    message = {"role": self.role}
    if self.content is not None:
        message["content"] = self.content
    if self.tool_calls is not None:
        message["tool_calls"] = [tc.dict() for tc in self.tool_calls]
    if self.name is not None:
        message["name"] = self.name
    if self.tool_call_id is not None:
        message["tool_call_id"] = self.tool_call_id
    if self.base64_image is not None:
        message["base64_image"] = self.base64_image
    return message
```

### 10.5.4 消息拼接

```python
def __add__(self, other) -> List["Message"]:
    """支持 Message + Message 或 Message + list"""
    if isinstance(other, list):
        return [self] + other
    elif isinstance(other, Message):
        return [self, other]

def __radd__(self, other) -> List["Message"]:
    """支持 list + Message"""
    if isinstance(other, list):
        return other + [self]
```

## 10.6 Memory 记忆模型

```python
class Memory(BaseModel):
    """Agent 记忆存储"""

    messages: List[Message] = []  # 消息列表
    max_messages: int = 100       # 最大消息数

    def add_message(self, message: Message) -> None:
        """添加消息"""
        self.messages.append(message)
        # 超过限制时删除旧消息
        if len(self.messages) > self.max_messages:
            self.messages = self.messages[-self.max_messages:]

    def add_messages(self, messages: List[Message]) -> None:
        """批量添加消息"""
        self.messages.extend(messages)
        if len(self.messages) > self.max_messages:
            self.messages = self.messages[-self.max_messages:]

    def clear(self) -> None:
        """清空记忆"""
        self.messages.clear()

    def get_recent_messages(self, n: int) -> List[Message]:
        """获取最近的 n 条消息"""
        return self.messages[-n:]

    def to_dict_list(self) -> List[dict]:
        """转换为字典列表"""
        return [msg.to_dict() for msg in self.messages]
```

## 10.7 ToolResult 工具结果模型

**文件位置**：`app/tool/base.py`

```python
class ToolResult(BaseModel):
    """工具执行结果"""

    output: Any = None                    # 正常输出
    error: Optional[str] = None          # 错误信息
    base64_image: Optional[str] = None   # Base64 截图
    system: Optional[str] = None         # 系统信息

    def __bool__(self):
        """判断是否有内容"""
        return any(getattr(self, field) for field in self.__fields__)

    def __str__(self):
        """字符串表示"""
        return f"Error: {self.error}" if self.error else self.output

    def __add__(self, other: "ToolResult") -> "ToolResult":
        """合并多个结果"""
        def combine_fields(field, other_field, concatenate=True):
            if field and other_field:
                if concatenate:
                    return field + other_field
                raise ValueError("Cannot combine tool results")
            return field or other_field

        return ToolResult(
            output=combine_fields(self.output, other.output),
            error=combine_fields(self.error, other.error),
            base64_image=self.base64_image or other.base64_image,
            system=combine_fields(self.system, other.system),
        )
```

### 10.7.1 子类

```python
class CLIResult(ToolResult):
    """可渲染为 CLI 输出的结果"""

class ToolFailure(ToolResult):
    """表示失败的工具结果"""
```

## 10.8 消息流转示例

```
用户输入
    │
    ▼
Message.user_message("帮我搜索 Python 教程")
    │
    ▼
Agent.think()
    │
    ▼
LLM.ask_tool() ←── messages + tools
    │
    ▼
Response with tool_calls
    │
    ├──► Message.assistant_message(content="我来帮你搜索...")
    │    │
    │    └──► Message.from_tool_calls(tool_calls=[...])
    │
    ▼
Agent.act()
    │
    ▼
WebSearch.execute(query="Python 教程")
    │
    ▼
ToolResult(output="搜索结果...")
    │
    ▼
Message.tool_message(output, tool_call_id, name)
    │
    ▼
Memory.add_message(tool_msg)
    │
    ▼
继续循环...
```

## 10.9 消息格式转换

```python
# Message → dict (用于 LLM API)
message.to_dict()
# {'role': 'user', 'content': 'Hello'}

# dict → Message
message = Message(**{'role': 'user', 'content': 'Hello'})

# Memory → dict list
memory.to_dict_list()
# [{'role': 'user', 'content': 'Hello'}, ...]
```
